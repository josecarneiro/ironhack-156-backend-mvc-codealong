<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="/">IronEmoji</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        {{#unless user}}
          <li class="nav-item">
            <a class="nav-link" href="/authentication/sign-in">Sign In</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/authentication/sign-up">Sign Up</a>
          </li>
        {{else}}
          <li class="nav-item">
            <a class="nav-link" href="/publication/create">Create Publication</a>
          </li>
          <form method="POST" action="/authentication/sign-out">
            <button class="btn nav-link">Sign Out</button>
          </form>
          {{!-- <a class="nav-link" href="/profile/{{ user.username }}">{{ user.username }}</a> --}}
        {{/unless}}
      </ul>
    </div>
  </div>
</nav>